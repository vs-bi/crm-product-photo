[project]
name = "proj-crm-photo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "python-dotenv",
    "loguru",
    "pillow",
    "anyio",
    "asyncer",
    "tqdm",
    "pandas",
    "sqlalchemy",
    "pymysql",
    "streamlit>=1.58.0",
]
